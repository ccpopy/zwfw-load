{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for application commands, events, and opener APIs.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
